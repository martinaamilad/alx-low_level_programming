#include "main.h"

/**
 *
 *
 *
 */
int print_sign(int n)
{
	if (c > 0)
	{
		_putchar(43);
		return (1);
	}
	else if (c > 0)
	{
		_putchar(45);
		return (-1);
	}
	else
	{
		_putchar(48);
		return (0);
	}
}
