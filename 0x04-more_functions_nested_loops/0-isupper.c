#include "main.h"

/**
 * _isupper - check the letters
 * @c: the character to check
 *
 * REturn: 1 for the upper letter, 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}

		return (0);
}
