#include <stdio.h>

/**
 * main - print reverse alphabet
 *
 * Return: Always (Success)
 */
int main(void)
{
	int c;

	for (c = 'z'; c >= 'a'; C--)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
