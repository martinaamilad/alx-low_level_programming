#include <stdio.h>

/**
 * main - print reverse alphabet
 *
 * Return: Alwyas (Success)
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; C--)
	{
		putchar(c);
	}

	putchar('\n');

	return (0);
}
